sudo: true
dist: xenial
language: cpp

services:
    - docker

before_install:
    - git clone https://github.com/tue-robotics/tue-env.git tue-robotics/tue-env
    - cd tue-robotics/tue-env

jobs:
  include:
    - stage: build docker image
      script: ci/build-docker-image.sh
