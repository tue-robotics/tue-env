sudo: true
dist: xenial
language:
  - cpp
  - shell

services:
    - docker

matrix:
  include:
    - language: cpp
      services: docker
      script: ci/build-docker-image.sh
    - language: shell
      script: bash -c 'shopt -s globstar; shellcheck **/*.bash **/*.sh'
